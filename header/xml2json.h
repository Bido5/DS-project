#pragma once
#include <iostream>
#include <string>
#include <queue>
#include <string>
#include <stack>
#include"XmlToTree.h"
using namespace std;

/*
*Function to prettify the XML file by keeping the indentation for each level
*/
string insert(string s, int n);
void xmltojson(string* file);
//string xmltojson(Node* node);
